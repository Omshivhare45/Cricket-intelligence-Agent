import React from 'react'
import ReactDOM from 'react-dom/client'
import CricketAgent from '../cricket-agent.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CricketAgent />
  </React.StrictMode>,
)